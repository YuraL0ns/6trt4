# Диагностический отчет: Медленная обработка фотографий

**Дата:** 2026-01-13  
**Проблема:** Анализ 6к фотографий идет очень медленно (сутки прошли, только 81% готово)

## 🔍 Найденные проблемы

### 1. КРИТИЧЕСКАЯ ПРОБЛЕМА: Solo Pool в Celery

**Местоположение:**
- `docker-compose.production.yml:206` - команда запуска: `-P solo`
- `fastapi/tasks/celery_app.py:182` - конфигурация: `worker_pool='solo'`

**Проблема:**
- Solo pool обрабатывает задачи **последовательно, одна за другой**
- Нет параллелизма - только один процесс, один поток
- При 8 CPU ядрах используется только 1 ядро (12.5% ресурсов)

**Влияние на производительность:**
- Одна фотография обрабатывается ~35 секунд
- 6,000 фотографий × 35 секунд = 210,000 секунд = **58.3 часа**
- Текущий прогресс (81% за сутки) соответствует этой оценке

### 2. Время обработки одной фотографии

**Из логов:**
```
Photo a0d1a4a5-a2b5-47f6-beea-4a825525db46: All processing steps completed successfully in 34.68 seconds
```

**Разбивка по шагам (примерная):**
- Remove EXIF: ~1-2 секунды
- Face search (InsightFace): ~20-25 секунд (самый медленный шаг)
- Watermark: ~2-3 секунды
- Number search: ~5-7 секунд
- Прочее (БД, файлы): ~2-3 секунды

### 3. Использование ресурсов

**CPU:**
- Celery контейнер: 99.89% (но только 1 ядро из 8)
- FastAPI контейнер: 404.45% (4 воркера работают нормально)

**Память:**
- Celery: 2.316 GiB / 11.68 GiB (19.83%)
- FastAPI: 2.776 GiB / 11.68 GiB (23.77%)

**Вывод:** CPU используется неэффективно - только 1 ядро из 8.

### 4. Почему используется Solo Pool?

**Причина (из комментариев в коде):**
```python
# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Используем solo pool вместо prefork
# InsightFace, OpenCV и ONNX Runtime не работают с prefork
# solo = один процесс, один поток (безопасно для ML библиотек)
```

**Проблема:** Prefork pool действительно может вызывать проблемы с ML библиотеками (InsightFace, OpenCV, ONNX Runtime), но solo pool слишком медленный.

## 💡 Рекомендации по решению

### Вариант 1: Threads Pool (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- Параллельная обработка нескольких фотографий
- Совместим с ML библиотеками (InsightFace, OpenCV)
- Использует все доступные CPU ядра
- Не требует изменений в коде обработки

**Изменения:**
1. В `docker-compose.production.yml:206`:
   ```yaml
   command: celery -A tasks.celery_app worker --loglevel=info -P threads --concurrency=8 --queues=celery,high_priority,default,low_priority
   ```

2. В `fastapi/tasks/celery_app.py:182`:
   ```python
   worker_pool='threads',  # Используем threads вместо solo для параллелизма
   ```

**Ожидаемое улучшение:**
- При 8 потоках: 58.3 часа / 8 = **~7.3 часа** (вместо 58 часов)
- Ускорение в **8 раз**

### Вариант 2: Несколько Celery воркеров с Solo Pool

**Преимущества:**
- Безопасно для ML библиотек
- Простое решение

**Недостатки:**
- Требует запуска нескольких контейнеров
- Больше потребление памяти

**Изменения:**
Запустить 4-8 контейнеров celery с solo pool, каждый обрабатывает свою очередь.

### Вариант 3: Оптимизация обработки одной фотографии

**Потенциальные улучшения:**
1. Кеширование моделей InsightFace (уже используется singleton)
2. Батчинг операций с БД
3. Асинхронная обработка независимых шагов

**Ожидаемое улучшение:**
- Сокращение времени обработки с 35 до 20-25 секунд
- Ускорение в **1.4-1.75 раза**

## 📊 Текущая статистика

- **Всего фотографий:** 6,278
- **Обработано:** 5,133 (81%)
- **Осталось:** 1,145
- **Время на одну фотографию:** ~35 секунд
- **Осталось времени (при текущей скорости):** ~11 часов
- **Общее время обработки:** ~58 часов

## ⚠️ Важные замечания

1. **Не останавливать текущий процесс** - он продолжает работать, просто медленно
2. **Solo pool был выбран не случайно** - для совместимости с ML библиотеками
3. **Threads pool должен работать** с InsightFace/OpenCV, но требует тестирования
4. **После изменений** нужно перезапустить только контейнер celery (не весь стек)

## 🔧 План действий (после завершения текущего анализа)

1. Протестировать threads pool на небольшом событии (10-20 фото)
2. Если threads pool работает стабильно - применить изменения
3. Мониторить логи на предмет ошибок ML библиотек
4. При необходимости вернуться к solo pool и использовать вариант 2

## 📝 Дополнительные наблюдения

- FastAPI работает нормально (4 воркера, 404% CPU)
- База данных и Redis работают стабильно
- Проблема только в Celery worker pool
- Логи не показывают ошибок, только медленную работу
