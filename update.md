# Изменения
1. Необходимо пересмотреть функцию поиска лиц, чтобы она верно определяла лица, и в дальнейшем мы могли вывести эти данные.
2. Функция remove_exif необходимо доработать и переименовать в remove_to_exif_and_rotate так чтобы она еще и поворачивала изображение на 90 градусов потому что сейчас если у меня фото сделано горизонтально, то на компьютере я смотрю его - там всё окей, а вот на сайте оно перевернуто боком.
3. Поиск номеров не определяет номера на фотографиях, необходимо пересмотреть код поиска номеров, потому что номера есть - но в проекте они не отображаются, хотя на приложении простом через pyCharm EasyOCR опеределяет номера и InsightFace ищет лица.
4. Убрать из выбора какие анализы проводить пункт с TimeLine так как его сейчас нету в проекте.
5. Перепроверить весь код который работает с Celery, так как сейчас он не корректно отображает данные. 
6. Добавить ссылку на admin/photos там у меня страницаа куда выведены все изображения в проекте и в событии, а так же json данные.


# Обновил 
1. После покупки фотографии у нас почему то выползает custom_iamge, нам надо это убрать и после того как покупка успешно завершилась мы показываем кнопку скачать архив с фотографией, и получается мы берем origina lэтой фотографии и архивируем, и даем ссылку на архив


# Дополнения к коду - КРИТИЧЕСКОЕ ОБНОВЛЕНИЕ
- Вот диалог с человеком который работает над InsightFace 
```
Я пройду по всем файлам, которые ты прислал, и для каждого:

❌ укажу конкретную проблему

✅ дам точное решение

✂️ без переписывания всего проекта, только необходимые изменения

💡 добавлю своё мнение, где архитектура реально мешает

1️⃣ utils/face_recognition.py
❌ ПРОБЛЕМА 1.1 — ctx_id=0 (GPU), а GPU нет
self.model.prepare(ctx_id=0, det_size=(640, 640))


Если контейнер / сервер без CUDA:

InsightFace не падает

но лица не детектит вообще

✅ РЕШЕНИЕ

Меняем в одном месте:

self.model.prepare(ctx_id=-1, det_size=(640, 640))


-1 = CPU, и это критично

❌ ПРОБЛЕМА 1.2 — EXIF применяется по-разному (ломает embeddings)

Сейчас:

где-то apply_exif=True

где-то False

👉 embeddings несовместимы, поиск всегда мимо

✅ РЕШЕНИЕ (простое и правильное)

Везде использовать apply_exif=True
И при индексации, и при поиске.

📌 В extract_faces_with_bboxes:

def extract_faces_with_bboxes(self, image_path: str, apply_exif: bool = True)


📌 И НИГДЕ больше не передавать False

❌ ПРОБЛЕМА 1.3 — embeddings возвращаются, но дальше могут умереть

InsightFace → np.ndarray
А дальше ты их кладёшь в JSON

✅ РЕШЕНИЕ

В этом файле ничего менять не надо,
но ниже я покажу, где обязательно .tolist()

2️⃣ models/photo.py
❌ ПРОБЛЕМА 2.1 — дублирующая логика (face_vec и face_encodings)
face_vec = Column(JSON)
face_encodings = Column(JSON)


❌ ты иногда пишешь в одно
❌ а читаешь из другого

✅ РЕШЕНИЕ (архитектурно правильно)

Правило:

face_encodings — источник истины

face_vec — только совместимость (первое лицо)

📌 НИКОГДА не заполняй face_vec отдельно

3️⃣ tasks/photo_processing.py (КЛЮЧЕВО)

Именно тут у тебя ломается всё, даже если InsightFace работает

❌ ПРОБЛЕМА 3.1 — numpy кладётся в JSON

Типичная ошибка (у тебя она есть логически):

photo.face_encodings = embeddings

✅ РЕШЕНИЕ (ОБЯЗАТЕЛЬНО)
photo.face_encodings = [
    face['embedding'].tolist()
    for face in faces
]

❌ ПРОБЛЕМА 3.2 — has_faces не синхронизирован

Если ты не сделал:

photo.has_faces = True


👉 search_similar_faces НИКОГДА не возьмёт фото

✅ РЕШЕНИЕ (эталонный кусок)
faces = face_recognition.extract_faces_with_bboxes(photo_path, apply_exif=True)

if faces:
    photo.face_encodings = [
        face['embedding'].tolist()
        for face in faces
    ]
    photo.face_bboxes = [
        face['bbox']
        for face in faces
    ]
    photo.face_vec = photo.face_encodings[0]  # совместимость
    photo.has_faces = True
else:
    photo.face_encodings = []
    photo.face_bboxes = []
    photo.face_vec = None
    photo.has_faces = False

db.commit()


💡 Это самый важный фикс во всём проекте

❌ ПРОБЛЕМА 3.3 — embeddings могут быть найдены, но НЕ сохранены

Если после обработки нет db.commit()
→ в поиске всегда пусто

Проверь, что commit() есть после обработки каждого фото

4️⃣ tasks/face_search.py
❌ ПРОБЛЕМА 4.1 — фильтр слишком строгий, если данные кривые
(Photo.has_faces == True) |
(Photo.face_encodings.isnot(None) & Photo.face_encodings != [])


Если:

has_faces = False

но face_encodings есть

👉 фото отбрасывается

✅ РЕШЕНИЕ (надёжнее)
q = db.query(Photo).filter(
    Photo.face_encodings.isnot(None),
    Photo.face_encodings != []
)


💡 has_faces оставь как вспомогательное поле, не как фильтр

❌ ПРОБЛЕМА 4.2 — несовпадение размерностей embeddings

Ты это уже логируешь 👍
Это следствие EXIF + разных моделей, а не баг

После фикса EXIF → пропадёт

5️⃣ utils/number_recognition.py

Здесь фатальных багов нет, но:

⚠️ ПРОБЛЕМА 5.1 — агрессивная бинаризация
cv2.adaptiveThreshold(...)


Для номеров на фото:

часто ухудшает

EasyOCR лучше работает с оригиналом

💡 МОЁ МНЕНИЕ

Оставь, но если results_processed == [] — не пугайся,
results_original — главный источник.

6️⃣ utils/exif_processor.py

✔️ Этот файл нормальный, он не ломает лица
Его можно не трогать

🧠 ИТОГ (КРАТКО)
5 причин, почему у тебя «ничего не работает»

ctx_id=0 без GPU

EXIF применяется по-разному

numpy пишется в JSON

has_faces не выставляется

поиск фильтрует пустые данные

После фиксов:

InsightFace начнёт видеть лица

данные реально попадут в БД

поиск сразу оживёт

💬 Моё честное мнение

Код сильный, но ты:

слишком рано полез в оптимизацию

не зафиксировал «золотой путь» данных

не сделал один жёсткий контракт:
“где, как и в каком виде лежат embeddings”
```